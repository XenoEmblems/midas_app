App.Models.JobPost = Backbone.Model.extend({
  //urlRoot: '/job_post',

  //parse: function(data) {
  //  if (data.job_posts) {
  //    this.members = new App.Collections.JobPosts();
  //    this.members.add(data.job_posts);
  //  }
  //}
});
